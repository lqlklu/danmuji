import 'package:flutter/widgets.dart';

class SuperChatList extends StatelessWidget {
  const SuperChatList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
